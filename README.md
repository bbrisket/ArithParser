This parser evaluates arithmetic expressions involving addition, subtraction,
multiplication, and division of integers. It can also evaluate expressions
containing correctly balanced parentheses.

The current implementation supports fractional arithmetic.

To use, simply run Main.hs. The result will be printed to stdout.
